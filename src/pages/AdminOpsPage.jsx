import { useEffect, useState } from 'react';
import { useParams } from 'react-router-dom';
import Layout from '../components/Layout.jsx';
import { subscribe, readDb, writeDb } from '../services/storage';
export default function AdminOpsPage(){ const {roomId}=useParams(); const [tick,setTick]=useState(0); useEffect(()=>subscribe(()=>setTick(x=>x+1)),[]); const db=readDb(); const room=db.rooms[roomId]; if(!room) return <Layout><div className="card">방 정보를 찾을 수 없습니다.</div></Layout>; const backup=()=>{const blob=new Blob([JSON.stringify(room,null,2)],{type:'application/json'}); const a=document.createElement('a'); a.href=URL.createObjectURL(blob); a.download=`${roomId}_backup.json`; a.click();}; return <Layout roomId={roomId}><section className="card"><h2>운영 안정화</h2><p>현재 라운드: {room.roomProgress.currentRoundId} · 단계: {room.roomProgress.currentPhase}</p><p>참가자: {Object.keys(room.players).length}명 · 팀: {Object.keys(room.teams).length}개</p><div className="actions"><button onClick={backup}>JSON 백업 다운로드</button><button onClick={()=>alert('데이터 건강도: 기본 room, teams, progress 확인 완료')}>데이터 건강도 점검</button></div><h3>Host Actions</h3>{room.hostActions.map(a=><p key={a.actionId}>• {new Date(a.createdAt).toLocaleString()} · {a.description}</p>)}</section></Layout>}
